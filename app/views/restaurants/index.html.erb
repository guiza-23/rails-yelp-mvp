<h1>Restaurants avilable</h1>

<ul>
  <% @restaurants.each do |r| %>
    <li><%= link_to r.name, restaurant_path(r) %></li>
  <% end %>
</ul>

<%= link_to 'add new restaurant', new_restaurant_path %>
